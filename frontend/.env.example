# ---------------------------------------------------------------------------
# AegisOps frontend — environment template
# Copy to `frontend/.env.local` and fill in LOCAL values. Never commit real env.
# Placeholders only. Only NEXT_PUBLIC_* values are exposed to the browser.
# ---------------------------------------------------------------------------

# Base URL of the AegisOps backend API (local default).
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000
